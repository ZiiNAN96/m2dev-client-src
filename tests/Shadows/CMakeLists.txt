add_executable(ShadowAmbientTest ShadowAmbientTest.cpp)
target_link_libraries(ShadowAmbientTest PRIVATE M2GraphicsSettings)
foreach(mode Config Splits Sun Stabilization Reconstruction Boundary)
    add_test(NAME Shadows.${mode} COMMAND ShadowAmbientTest ${mode})
    set_tests_properties(Shadows.${mode} PROPERTIES LABELS "shadow;ambient;portable;fast;g34x" TIMEOUT 15)
endforeach()
if(M2_BUILD_WINDOWS_CLIENT)
    add_executable(ShadowAmbientRenderTest ShadowAmbientRenderTest.cpp)
    get_target_property(shadow_diligent_includes M2RendererDiligent INCLUDE_DIRECTORIES)
    target_include_directories(ShadowAmbientRenderTest PRIVATE ${shadow_diligent_includes})
    target_link_libraries(ShadowAmbientRenderTest PRIVATE M2RendererDiligent M2Platform Diligent-GraphicsEngineD3D11Interface)
    add_test(NAME Shadows.Render COMMAND ShadowAmbientRenderTest)
    set_tests_properties(Shadows.Render PROPERTIES LABELS "shadow;ambient;gpu;fast;g34x" TIMEOUT 60 RUN_SERIAL TRUE)
endif()
