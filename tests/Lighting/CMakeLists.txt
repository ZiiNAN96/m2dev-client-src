add_executable(SceneLightingTest SceneLightingTest.cpp)
target_link_libraries(SceneLightingTest PRIVATE M2GraphicsSettings)
foreach(mode Validation MapConversion Revision Settings)
    add_test(NAME Lighting.${mode} COMMAND SceneLightingTest ${mode})
    set_tests_properties(Lighting.${mode} PROPERTIES LABELS "lighting;portable;fast;g2x" TIMEOUT 15)
endforeach()
add_test(NAME Lighting.HeaderBoundary COMMAND ${CMAKE_COMMAND} -DROOT=${CMAKE_SOURCE_DIR} -P ${CMAKE_CURRENT_SOURCE_DIR}/HeaderBoundary.cmake)
set_tests_properties(Lighting.HeaderBoundary PROPERTIES LABELS "lighting;portable;fast;g2x" TIMEOUT 15)
if(M2_BUILD_WINDOWS_CLIENT)
    add_executable(LightingRenderTest LightingRenderTest.cpp)
    target_link_libraries(LightingRenderTest PRIVATE M2RendererDiligent M2Platform)
    add_test(NAME Lighting.Render COMMAND LightingRenderTest)
    add_test(NAME Lighting.MaterialBalls COMMAND MaterialRenderTest "${CMAKE_CURRENT_SOURCE_DIR}/fixtures/material_balls.glb" --lighting)
    set_tests_properties(Lighting.Render Lighting.MaterialBalls PROPERTIES LABELS "lighting;gpu;fast;g2x" TIMEOUT 30 RUN_SERIAL TRUE)
endif()
