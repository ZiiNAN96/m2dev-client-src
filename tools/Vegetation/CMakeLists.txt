if(NOT WIN32)
    message(FATAL_ERROR "The retained legacy SPT extraction binary requires Windows; VegetationRuntime does not.")
endif()
if(NOT TARGET SpeedTree)
    add_subdirectory(${CMAKE_SOURCE_DIR}/extern/library/SpeedTree ${CMAKE_CURRENT_BINARY_DIR}/legacy-sdk)
endif()
add_executable(ZiiNANSPTProbe LegacySPTProbe.cpp)
target_link_libraries(ZiiNANSPTProbe PRIVATE SpeedTree user32 M2FirstPartyArchitectureChecks)
target_include_directories(ZiiNANSPTProbe SYSTEM PRIVATE ${CMAKE_SOURCE_DIR}/extern/include)
if(NOT TARGET AssetToolCore)
    message(FATAL_ERROR "Enable M2_BUILD_ASSET_TOOL to reuse the existing GLB writer for SPT conversion.")
endif()
add_library(LegacySPTExtraction STATIC LegacySPTExtractor.cpp)
target_include_directories(LegacySPTExtraction PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
target_link_libraries(LegacySPTExtraction PRIVATE SpeedTree user32 PUBLIC VegetationRuntime AssetToolCore)
add_executable(ZiiNANVegetationTool Main.cpp)
target_link_libraries(ZiiNANVegetationTool PRIVATE LegacySPTExtraction M2FirstPartyArchitectureChecks)
add_test(NAME Vegetation.ConverterFailures COMMAND ${CMAKE_COMMAND}
    -DTOOL=$<TARGET_FILE:ZiiNANVegetationTool> -DOUTPUT=${CMAKE_CURRENT_BINARY_DIR}/failures-$<CONFIG>
    -P ${CMAKE_SOURCE_DIR}/tests/Vegetation/ConverterFailureTest.cmake)
set_tests_properties(Vegetation.ConverterFailures PROPERTIES TIMEOUT 25 LABELS "vegetation;offline;fast;hx")
if(M2_BUILD_WINDOWS_CLIENT)
    add_executable(VegetationPackAudit PackAudit.cpp)
    target_link_libraries(VegetationPackAudit PRIVATE PackLib EterLib EterBase M2FirstPartyArchitectureChecks)
    get_target_property(vegetation_client_libraries UserInterface LINK_LIBRARIES)
    add_executable(VegetationRenderParity ../../tests/Vegetation/RenderParityTest.cpp)
    target_link_libraries(VegetationRenderParity PRIVATE ${vegetation_client_libraries} M2RendererDiligent VegetationRuntime)
    add_test(NAME Vegetation.RenderParity COMMAND VegetationRenderParity ${CMAKE_SOURCE_DIR}/../m2dev-client
        ${CMAKE_SOURCE_DIR}/build/hx/compiled ${CMAKE_SOURCE_DIR}/build/hx/visual-$<CONFIG>)
    set_tests_properties(Vegetation.RenderParity PROPERTIES TIMEOUT 120 RUN_SERIAL TRUE LABELS "vegetation;gpu;real-assets;fast;hx")
endif()
